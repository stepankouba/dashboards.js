'use strict';
/**
 *	DBS example app
 */

// Declare app level module which depends on filters, and services
angular.module('dbsApp.services.local', []).
factory('Local', ['$rootScope', '$log', function($rootScope, $log){
	return {
		// TODO  implementation
	};
}]);
